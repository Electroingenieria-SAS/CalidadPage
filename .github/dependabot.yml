version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    groups:
      next-runtime:
        patterns:
          - next
          - eslint-config-next
      production-dependencies:
        dependency-type: production
