/**
 * Legacy compatibility placeholder.
 *
 * The production application is Next.js and Vercel must build it through
 * `npm run build`. This file intentionally has no Vite imports so a stale
 * tracked file cannot pull Vinext/Cloudflare dependencies into type checking.
 */
const legacyViteConfig = {};
export default legacyViteConfig;
